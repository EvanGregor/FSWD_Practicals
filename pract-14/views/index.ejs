<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Resume Upload</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: linear-gradient(135deg, #0400ff, #ff0000);
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .card {
      background: #fff;
      padding: 35px 30px;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0,0,0,0.08);
      width: 360px;
      text-align: center;
    }
    h2 {
      margin-bottom: 20px;
      color: #222;
      font-size: 22px;
    }
    input[type=file] {
      margin: 20px 0;
      border: 1px solid #ccc;
      padding: 10px;
      border-radius: 6px;
      width: 100%;
      font-size: 14px;
      background: #fafafa;
    }
    button {
      background: #007BFF;
      color: #fff;
      border: none;
      padding: 12px;
      border-radius: 6px;
      cursor: pointer;
      font-size: 15px;
      width: 100%;
    }
    button:hover {
      background: #0056b3;
    }
    .success {
      color: #28a745;
      margin-bottom: 15px;
      font-size: 14px;
    }
    .error {
      color: #dc3545;
      margin-bottom: 15px;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="card">
    <h2>Upload Your Resume</h2>

    <% if (message) { %>
      <p class="success"><%= message %></p>
    <% } %>

    <% if (error) { %>
      <p class="error"><%= error %></p>
    <% } %>

    <form action="/upload" method="POST" enctype="multipart/form-data">
      <input type="file" name="resume" id="resume" 
             accept=".pdf,.doc,.docx" required>
      <button type="submit">Upload Resume</button>
    </form>
  </div>
</body>
</html>
